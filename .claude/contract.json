{
  "stop": {
    "cmd": "bin/gauntlet stop",
    "why": "read and run by .claude/hooks/stop-gate.py, dispatched via ~/.claude/settings.json's Stop hook through .claude/hooks/dispatch.py to .claude/hooks/roster.json's Stop entry"
  },
  "test": {
    "cmd": "npm test",
    "why": "package.json#scripts.test (vitest run .Workflow .claude)"
  },
  "test_one": {
    "cmd": "npx vitest run <file>",
    "why": "vitest's single-file form; no package.json script for it"
  },
  "test_related": {
    "cmd": "npx vitest related --run <file>",
    "why": "vitest's related-tests form; no package.json script for it"
  },
  "typecheck": {
    "cmd": "npm run typecheck",
    "why": "package.json#scripts.typecheck (tsc --noEmit, incremental per tsconfig.json)"
  },
  "lint": {
    "cmd": "npm run lint",
    "why": "package.json#scripts.lint (eslint, knip, dependency-cruiser)"
  },
  "lint_one": {
    "cmd": "npx eslint <file>",
    "why": "eslint's per-file form; no package.json script for it"
  },
  "rules": {
    "cmd": "bin/lint",
    "why": "bin/lint, the grep rules ratified under docs/adr/0174-a-ratified-standard-must-be-expressible-as-a-sub-second-grep.md"
  },
  "clones": {
    "cmd": "npm run clones",
    "why": "package.json#scripts.clones (jscpd, configured under package.json#jscpd)"
  },
  "adrs": {
    "cmd": "npm run adrs",
    "why": "package.json#scripts.adrs (tsx .Workflow/agent-workflows/shared/adr-index.cli.ts .)"
  },
  "drift": {
    "cmd": "npm run drift",
    "why": "package.json#scripts.drift (tsx .Workflow/agent-workflows/shared/prose-drift.cli.ts .)"
  },
  "all": {
    "cmd": "npm run check",
    "why": "package.json#scripts.check (bin/gauntlet push)"
  }
}
