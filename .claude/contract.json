{
  "stop": {
    "cmd": "bin/gauntlet stop",
    "why": ".claude/hooks/gauntlet.sh#check-command, wired at .claude/settings.json#hooks.Stop"
  },
  "test": {
    "cmd": "npm test",
    "why": "package.json#scripts.test (vitest run .Workflow .claude && npm run clone:check)"
  },
  "test_one": {
    "cmd": "npx vitest run <file>",
    "why": "vitest's single-file form; no package.json script for it"
  },
  "test_related": {
    "cmd": "npx vitest related --run <file>",
    "why": "vitest's related-tests form; no package.json script for it"
  },
  "typecheck": {
    "cmd": "npm run typecheck",
    "why": "package.json#scripts.typecheck (tsc --noEmit)"
  },
  "lint": {
    "cmd": "npm run lint",
    "why": "package.json#scripts.lint (eslint .)"
  },
  "all": {
    "cmd": "npm run check",
    "why": "package.json#scripts.check (bin/gauntlet push && npm run clone:check)"
  }
}
