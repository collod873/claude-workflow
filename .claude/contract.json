{
  "stop": {
    "cmd": null,
    "why": "no turn-end check narrower than the full checks found at .claude/hooks/stop-gate.sh"
  },
  "test": {
    "cmd": "npm test",
    "why": "package.json#scripts.test (vitest run)"
  },
  "test_one": {
    "cmd": "npx vitest run <file>",
    "why": "vitest's single-file form; no package.json script for it"
  },
  "typecheck": {
    "cmd": "npm run typecheck",
    "why": "package.json#scripts.typecheck (tsc --noEmit)"
  },
  "lint": {
    "cmd": "npm run lint",
    "why": "package.json#scripts.lint (eslint .)"
  },
  "all": {
    "cmd": null,
    "why": "no aggregate script found among package.json#scripts.{check,verify,ci}"
  }
}
