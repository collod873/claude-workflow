{
  "stop": {
    "cmd": ".claude/hooks/gauntlet.sh stop",
    "why": ".claude/settings.json#hooks.Stop (\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/gauntlet.sh stop)"
  },
  "test": {
    "cmd": "npm test",
    "why": "package.json#scripts.test (vitest run)"
  },
  "test_one": {
    "cmd": "npx vitest run <file>",
    "why": "vitest's single-file form; no package.json script for it"
  },
  "typecheck": {
    "cmd": "npm run typecheck",
    "why": "package.json#scripts.typecheck (tsc --noEmit)"
  },
  "lint": {
    "cmd": "npm run lint",
    "why": "package.json#scripts.lint (eslint .)"
  },
  "all": {
    "cmd": "npm run check",
    "why": "package.json#scripts.check (bin/gauntlet push)"
  }
}
