[{"finding":"An exemption from an automated reachability/dead-code gate must carry an inline prose reason at the exemption site itself, never a silent name in an ignore list","decision":"declined","sites":[".Workflow/agent-workflows/shared/check-contract.ts:57",".Workflow/agent-workflows/shared/check-contract.ts:93","knip.config.ts:79"],"reason":"This is already a ratified entry: CODING_STANDARDS.md line 26, \\\"Exemptions say why, in place\\\" — same scope (reachability/dead-code gate), same requirement (prose at the exempted export, knip's @shell/@fixture tags), same red flag (a bare name in an ignore list or baseline). A second entry would be a duplicate in a file whose stated job is staying small. Mechanising it instead is not open either: the requirement is that prose exist at the exempted *export*, which lives in a different file from the config that exempts it, so no ESLint rule can see both halves — and the three cited sites are the standard being obeyed (the @fixture tag at check-contract.ts:57, the @shell tag at :93, and the tags block at knip.config.ts:79 that explains why both hatches demand prose), so a rule could not reproduce the evidence it would be tried against. Nothing edited."},{"finding":"A baseline that grandfathers existing debt into a new gate must only ratchet down and gate solely on new additions, never re-grow or serve as a permanent allowlist","decision":"declined","sites":[".Workflow/agent-workflows/shared/wiring-baseline.json:3","docs/agents/clone-gate.md:46"],"reason":"Already covered by a ratified entry, and the absolute form would contradict the repository's landed decisions. `CODING_STANDARDS.md`'s \"Zero-grandfather rails\" Red-flag line already carries this clause verbatim — \"that baseline only ratchets down, gating solely on new additions (`docs/agents/clone-gate.md` rule 5)\" — and cites one of the two sites as its home; a new entry would be a second live copy of it, which the file's own \"How this doc stays small\" rules out. Both sites are the standard being obeyed rather than breached: `wiring-baseline.json`'s `why` states the gate fires only on the delta and entries leave as each is wired or deleted, and `wiring-baseline.ts` enforces the shrink half via `regenerate && diff` so a resolved entry left in the file fails the push. The finding's \"never re-grow\" absolutism is also wrong for this repository: `clone-gate.md` rule 5 permits exactly two growths, both ratified — acceptance-lane clones nobody else may dedupe because `tests/acceptance/` is immutable (ADR-0114), and the re-cut carry-across where one entry is substituted for one and the count never rises (ADR-0116). Landing this as prose would set `CODING_STANDARDS.md` against both ADRs, and amending a decision means a new ADR, not a standards entry that quietly contradicts one. Mechanisation is unavailable: a ratchet is a property of a file across commits, invisible to a linter reading a single tree, which is why enforcement already lives in `wiring-baseline.ts` and the clone-gate runner. Nothing edited."},{"finding":"A \"Cite, don't restate\" trim names a plain code comment (not a helper docstring or an ADR) as the rationale's home","decision":"declined","sites":[".github/workflows/verify.yml:116",".Workflow/agent-workflows/fixer/fixer.test.ts:210",".Workflow/agent-workflows/fixer/fixer.test.ts:264"],"reason":"All three sites cite into workflow YAML — verify.yml:134 (\\\"that job's comment is the home for why\\\"), fixer.test.ts:502 (\\\"`fixer.yml`'s own header comment\\\") and fixer.test.ts:646 (\\\"that step's own comment\\\") — and that is the entire finding: a `.yml` file has no docstring to hold a rationale, so the comment beside the step is the only home available. The harm \\\"Cite, don't restate\\\" exists to prevent is absent at every site: there is one copy of each reason and the others point at it, and at fixer.test.ts:646 the pointer is machine-checked — the describe block below asserts verify.yml still contains `echo \\\"judging $PR on $BRANCH\\\"` verbatim (fixer.test.ts:665), so a reworded home reds the suite instead of dangling silently. The strict form would also be unfollowable and would make the repository worse: verify.yml is in the immutable set, so a standard outlawing its comment as a home leaves a permanent violation with no legal fix, and the only compliant alternatives are the two things already ruled out — restating the reason in a distant TypeScript docstring (what the existing entry forbids) or minting an ADR for \\\"why `fetch-depth: 0`\\\", far under docs/adr/README.md's bar for an architectural decision. Mechanisation failed independently of that: the rule would have to flag a YAML comment, ESLint does not lint YAML, and `.github/` may not be edited, so the rule could not reproduce the evidence that justified it. Nothing edited."},{"finding":"A function that can exit without landing a commit must call `restoreWorkingTree` on every such exit path before returning, or a later finding in the same batch inherits a dirty working tree from one that was supposed to be undone","decision":"declined","sites":[".Workflow/agent-workflows/ratify/ratifier.ts:142",".Workflow/agent-workflows/ratify/ratifier.ts:168",".Workflow/agent-workflows/ratify/ratifier.ts:177",".Workflow/agent-workflows/ratify/ratifier.ts:210"],"reason":"All four sites are calls to `restoreWorkingTree`, a helper defined in ratify/land.ts and called nowhere outside `ratifyBatch`/`ratifyOne` in one file — the gate counted a lane-local convention being correctly followed, not a defect recurring across the repository. It cannot be mechanised: whether an exit needs the restore depends on `commitWorkingTree` returning null meaning the tree already matched the parent (so ratifier.ts:222 correctly restores nothing), which is a fact about a helper's return value rather than a syntactic shape — and a rule that flags violations would flag none of these four compliant sites, so the trial would demote it by construction. It does not deserve a prose entry either: as written it is one lane's invariant that /standards-pass would sweep every unrelated batch against, and generalised to something repo-wide its red flag (\"an early return in a function that mutated something\") fires on nearly every function. The invariant is already documented once where it belongs, in `restoreWorkingTree`'s docstring and `ratifyBatch`'s."}]
