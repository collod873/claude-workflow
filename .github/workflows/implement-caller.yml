name: Implement

"on":
  repository_dispatch:
    types: [ticket-ready]

jobs:
  implement:
    permissions:
      contents: write
      pull-requests: write
      issues: write
    uses: collod873/claude-workflow/.github/workflows/implement.yml@main
    secrets: inherit
