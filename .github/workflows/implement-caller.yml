name: Implement
run-name: "Implement #${{ github.event.client_payload.issue }}"

"on":
  repository_dispatch:
    types: [ticket-ready]

jobs:
  implement:
    permissions:
      contents: write
      pull-requests: write
      issues: write
    uses: collod873/claude-workflow/.github/workflows/implement.yml@main
    secrets: inherit
