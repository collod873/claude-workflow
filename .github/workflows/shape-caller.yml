name: Shape

"on":
  issues:
    types: [labeled]
  issue_comment:
    types: [created]

jobs:
  shape:
    permissions:
      contents: read
      issues: write
    uses: collod873/claude-workflow/.github/workflows/shape.yml@main
    secrets: inherit
