name: Shape — accept (caller)

"on":
  issues:
    types: [labeled]

jobs:
  accept:
    permissions:
      contents: write
      issues: write
    uses: collod873/claude-workflow/.github/workflows/shape-accept.yml@main
