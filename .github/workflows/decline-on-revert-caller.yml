name: Decline on revert (caller)

"on":
  push:
    branches: [main]
    paths:
      - "CODING_STANDARDS.md"
      - "eslint.config.js"

jobs:
  decline:
    permissions:
      contents: write
    uses: collod873/claude-workflow/.github/workflows/decline-on-revert.yml@main
