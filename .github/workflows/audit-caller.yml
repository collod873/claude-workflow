name: Audit

"on":
  repository_dispatch:
    types: [session-captured]

jobs:
  audit:
    permissions:
      contents: write
      pull-requests: write
    uses: collod873/claude-workflow/.github/workflows/audit.yml@main
    secrets: inherit
