name: Ratify release

"on":
  pull_request:
    types: [closed]

jobs:
  ratify-release:
    permissions:
      contents: write
    uses: collod873/claude-workflow/.github/workflows/ratify-release.yml@main
