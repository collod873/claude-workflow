name: Back-stamp

"on":
  push:
    branches: [main]
    paths:
      - "docs/adr/**"
      - "docs/research/**"

jobs:
  stamp:
    permissions:
      contents: write
    uses: collod873/claude-workflow/.github/workflows/back-stamp.yml@main
