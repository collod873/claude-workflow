name: Record ratifications

"on":
  repository_dispatch:
    types: [ratifier-merged]

jobs:
  record-ratifications:
    permissions:
      contents: write
      pull-requests: read
    uses: collod873/claude-workflow/.github/workflows/record-ratifications.yml@main
