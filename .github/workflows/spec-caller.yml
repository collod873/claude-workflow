name: Spec

"on":
  issues:
    types: [labeled]
  repository_dispatch:
    types: [sheet-accepted]

jobs:
  spec:
    permissions:
      contents: write
      issues: write
    uses: collod873/claude-workflow/.github/workflows/spec.yml@main
    secrets: inherit
