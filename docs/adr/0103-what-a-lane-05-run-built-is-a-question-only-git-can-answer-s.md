---
status: note
date: 2026-08-29
amends: ADR-0042
reversal: Reverting the no-op check to the filesystem comparison and dropping the `IMPLEMENT_ANSWER_PATH` upload touches `implement.ts` and `implement.yml` and nothing that reads them; what cannot be retracted is run 33275876786's 23 minutes and $6.36, since a lane 05 answer exists only in memory until something writes it down.
---

# What a lane 05 run built is a question only git can answer, so the no-op check asks git and the run keeps a copy of its implementer's answer

Re-admitted 2026-08-31 as a **note**: this records an implementation note — how a tool or lane behaves, not a constraint
that binds later work.

It changes ADR-0042, which is where the change belonged.

The number and filename are kept unchanged because they are cited from issues and
permalinks that cannot be edited from this repo.
