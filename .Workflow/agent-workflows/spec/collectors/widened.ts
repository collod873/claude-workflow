import type { GhExec } from "../../shared/gh";
import type { DecidedContext } from "../author-contract";

export function collectWidenedContext(gh: GhExec, issueNumber: number): DecidedContext {
  throw new Error("#600: not built");
}
