I have what I need. Here's what the sweep found.

**The shape of the risk:** this spec's five build steps split into tickets that all touch the same three things — a session transcript, a `git` invocation, and a hook that must fail open — and none of those has a home in this repo yet. `.claude/hooks/` today holds exactly one mechanism (the gauntlet), `shared/` has a `GhExec` seam but no `GitExec`, and the "steal, do not re-derive" table points at Python sources while every gate in this repo (`bin/gauntlet` → tsc/eslint/vitest) only sees TypeScript. Left unsaid, capture and backfill would each grow their own transcript parser and their own capture-file writer, and the auditor, notes storage and release trigger would each hand-roll `git`.

<output>["Hook code convention — a `.claude/hooks/<name>.sh` shim (absolute `#!/bin/bash`, sources `bin/node-on-path.sh`, exits 0 on every path) + a `<name>.mjs` doing the work + a `<name>.test.ts` beside it — `.claude/hooks/`, the gauntlet's existing shape — port the Python sources to `.mjs` rather than adding Python, which `bin/gauntlet` cannot typecheck, lint or run; consumed by capture, backfill and the auditor.", "Transcript reader — one module that parses a session JSONL once and returns both the spine (extract.py's filter: `type == \"user\"`, drop `isMeta`/`isSidechain`, `origin.kind == \"human\"`, `promptSource` in None/typed/paste, strip `<system-reminder>`, skip `<local-command-*>`/`<command-*>`/`<bash-*>`, dedupe on `uuid`, plus assistant `text` blocks) and the session's touched paths (Edit/Write `file_path` + Bash `command` strings) — `.claude/hooks/lib/transcript.mjs` — consumed by capture, backfill and the auditor's diff scoping.", "Capture file — the path builder (`Knowledge-Base/raw/sessions/YYYY-MM-DD-{session_id[:8]}.md`), the frontmatter/section render, and the atomic lock + tmp + rename write, with an env var overriding the corpus root so tests never write into the real Knowledge-Base — `.claude/hooks/lib/capture-file.mjs` — consumed by the capture hook and the backfill, which must produce byte-identical files, and read by the auditor.", "Run log — `hookLog(name, ...fields)` and its bash twin `hook_log`, tab-separated, UTC, fail-open, one row per outcome including every give-up path, with an env var redirecting the log dir for tests — `.claude/hooks/lib/hook-log.mjs` and `.sh` — consumed by every hook half here; the bash shim logs its own skips because everything past the detached handoff has its stdio on /dev/null.", "`GitExec` — an injected `(args: string[]) => string` executor around `git`, mirroring `shared/gh.ts`'s `GhExec` exactly, plus an in-memory `git.fake.ts` recording every argv — `.Workflow/agent-workflows/shared/git.ts` — consumed by the SHA-range diff, git-notes read/write, and the release trigger's scope computation.", "`Observation` — the zod schema for one finding (lens `VIOLATION`|`PROPOSED`, the commit it is anchored to, its site list, body) and the one exported `observation()` fixture builder beside it, per CODING_STANDARDS — `.Workflow/agent-workflows/shared/observation-schema.ts` — consumed by the auditor's output parsing, the git-notes writer/reader, and the release PR renderer.", "`<output>` block contract — the auditor's `claude -p` reply ends in exactly one `<output>` block, extracted and schema-validated by the existing `extractOutput` — `.Workflow/agent-workflows/shared/output-block.ts` — use it rather than porting Lumaria's `parseBlocks`; consumed by the auditor.", "Binary-override stub seam — a `<THING>_BIN` env var read at spawn time (as `GAUNTLET_BIN` is today) plus `shared/claude-cli.stub.ts` for a stub `claude` first on PATH — `.claude/hooks/` and `.Workflow/agent-workflows/shared/` — consumed by every test standing in for `claude`, `git` or `node` so no test spawns a model.", "`withTempRepo()` — a real `git init` repo with seeded commits for one test, cleaned up via `onTestFinished`, built the way `shared/handoff-dir.fixture.ts` builds its temp dir — `.Workflow/agent-workflows/shared/temp-repo.fixture.ts` — consumed by the range-diff, git-notes and release-trigger tests.", "GitHub REST paths — any new `repos/{owner}/{repo}/...` path (the release PR, the PRD close read) is built through `shared/gh-paths.ts` and sent through `GhExec`/`createFakeGh`, never inlined — an eslint `no-restricted-syntax` rule fails the build on a hand-rolled one — consumed by the release trigger."]</output>
