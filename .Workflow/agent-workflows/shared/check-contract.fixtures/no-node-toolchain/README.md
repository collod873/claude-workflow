# Fixture: no Node toolchain

Deliberately has no `package.json` anywhere in this tree — the probe's every-slot-null case
(`docs/adr/0056-...md`: "the probe's honest limit is recorded rather than designed around").
