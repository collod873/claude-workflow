#!/bin/bash
# The conventional turn-end hook path, present so the probe can fall back to it — and, like any
# hook, naming the check it runs rather than standing in for one (#186).
#
# check-command: bin/checks.sh
exit 0
