name: Narrow

"on":
  workflow_dispatch:

permissions:
  contents: read
  issues: read

jobs:
  narrow:
    runs-on: ubuntu-latest
    steps:
      - name: Run the lane
        run: npx tsx src/lane.ts
