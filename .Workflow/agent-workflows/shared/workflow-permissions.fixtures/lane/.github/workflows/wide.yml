name: Wide

"on":
  workflow_dispatch:

jobs:
  wide:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - name: Run the lane
        run: npx tsx src/lane.ts
