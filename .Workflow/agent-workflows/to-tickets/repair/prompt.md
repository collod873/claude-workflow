The plan gate read the answer you just gave and refused it. Its refusal is below, every failing
check at once. This is the one repair round: the publisher runs this same gate before it files a
single issue, so an answer refused again ends the run with nothing published.

Fix exactly what the refusal names, in the plan you already drew. Do not re-slice, re-order or
re-word anything it does not name; the rest of your answer already passed. Where it names a path
without saying rooted where, spell that path in full from the repository root, or claim it in full
in that slice's own `filesClaimed` if the slice modifies it.

When you are done, answer with the `StructuredOutput` tool again, carrying the whole answer, not
only the slices you changed.

---

{{REFUSAL}}

---
