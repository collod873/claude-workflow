[{"finding":"When correctness can't be determined (ambiguous match, missing required precondition), refuse explicitly with an actionable error rather than proceeding on a guess or an inherited default","decision":"declined","sites":["bin/canary:645",".Workflow/agent-workflows/shared/vitest-json.ts:53"],"reason":"This finding is verbatim the already-ratified entry \"Refuse rather than guess\" (CODING_STANDARDS.md:42): \"when a precondition for a correct answer is missing or the match is ambiguous, stop with an error naming what is absent and what would fix it, instead of taking a candidate or inheriting an ambient default.\" Its Why line names these two sites' exact failure modes (\"the wrong run gets judged green and the wrong config gets tested\"), and its Red flag covers both shapes (\"a first-of-list pick where the list could hold more than one, or a fall-through to whatever the surrounding environment supplies when the thing itself was never found\"). bin/canary:645 (refusing when the fire matches more than one run instead of picking candidates[0]) and shared/vitest-json.ts:53 (refusing when the target has no config of its own instead of letting vitest climb to an ambient one) are compliant exemplars of that entry, not new evidence; the same batch carries a live VIOLATION filed against it, so it is already in force. Ratifying again would duplicate an existing standard and grow a file whose stated rule is to stay small."}]
