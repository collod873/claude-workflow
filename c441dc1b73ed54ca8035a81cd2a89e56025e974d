[{"finding":"**Pin a mandated copy to its source**: \"where a boundary forces a definition to be written twice, one test reads both texts and asserts they still say the same thing... Red flag: a constant, grammar or literal restated in a second file... that no 'agrees with the … it is a copy of' test names.\" The doc's prose restates the exact permissions literal (`contents: write, issues: write, actions: read`) that lives in `dispatch-reconcile.yml`'s `permissions:` block. The diff updates this restated copy by hand to stay in sync, but no test asserts the doc's literal agrees with the workflow YAML it describes — the sync is manual and will drift silently on the next permissions change.","decision":"ratified","sites":["docs/agents/reconcile-lane-edges.md:175"],"reason":"landed as \"Pin a mandated copy to its source\" in ratifier PR #485","landedAs":"Pin a mandated copy to its source"},{"finding":"**Fixtures through one builder**: \"a test builds a schema-typed fixture through one exported builder, never a hand-rolled literal; the builder lives beside the zod schema it constructs.\" Red flag: \"an object literal in a test that spells out every field of a schema.\" `LaneRun` is a zod schema (`z.object({ databaseId, displayTitle, status, conclusion, url })`) exported from `strikes.ts`, but its own test constructs seven object literals spelling out all five fields directly instead of through an exported builder taking `Partial<LaneRun>`.","decision":"ratified","sites":[".Workflow/agent-workflows/dispatch/strikes.test.ts:93"],"reason":"landed as \"Fixtures through one builder\" in ratifier PR #485","landedAs":"Fixtures through one builder"}]
